# 2026-08-22 13:52 UTC — старт поллинга

## Что запущено
- `raw_poller`, интервал 30 с, каталог `/api/providers` + 15 balance-эндпоинтов за цикл (16 строк/цикл).
- На строку пишется: время запроса/ответа (UTC, мкс), latency, HTTP-версия, код, reason, все заголовки,
  `Date` сервера отдельно, content-type, тело (text + b64 если не UTF-8), флаг «валидный JSON»,
  класс и текст исключения транспорта. Sink: SQLite + JSONL, падение одного не мешает другому.

## Уже известно из ручного осмотра (13:45–13:46)
- 15 провайдеров, 4 pay_model, 7 разных схем тела (см. диалог).
- Значения дрейфуют между опросами на 0.01–0.05 USD / 1 кредит за ~20 с; anthropic cost растёт,
  meta_ads 30d/24h spend *убывает* (скользящее окно?).
- Каталог: `name` ≠ `provider` (brightdata→"Oxylabs", openrouter→"Groq" и т.п.) — возможно, специально.

## Первая аномалия
- Цикл 1 (13:51:56): **twocaptcha → HTTP 500**, тело `{"error":"upstream 500"}`, latency 215 мс,
  content-type json. Остальные 15 — 200. Гипотеза: инъекция случайных сбоев; проверить частоту
  и распределение по провайдерам на следующей проверке.

## Что смотреть в 14:12
- Доля non-200 / transport-ошибок по провайдерам; бывают ли non-JSON тела, таймауты.
- Меняется ли схема тела одного провайдера между циклами.
- Стабильность каталога (состав, endpoint'ы, pay_model).
- Скачки значений (top-up вверх / резкое падение), ступенчатость vs плавный дрейф.
- Расхождение `Date` сервера и локального времени.
