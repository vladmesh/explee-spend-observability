# 2026-08-22 14:13 UTC — первые 20 минут (43 цикла, 688 строк)

Процесс жив (PID 3499841), 43 цикла × 16 строк, транспортных ошибок/таймаутов (15 с) — ноль.
Каталог `/api/providers` стабилен: 43/43 раз 200, одно и то же тело.

## Классы сбоев (всё — «данные», не падения)
| класс | тело | заголовки | latency | кто |
|---|---|---|---|---|
| 500 | `{"error":"upstream 500"}` json | обычные | ~220 мс | twocaptcha, scrapfly, meta_ads |
| 503 | `{"error":"upstream 503"}` json | обычные | ~220 мс | tremendous |
| 429 | `{"error":"rate limited"}` json | **`retry-after: 5`** | ~220 мс | findymail, tremendous |
| 504 | **Cloudflare HTML-страница** 6.4 КБ, `text/html` | `retry-after: 120`, cf-ray | **~3.2 с** | evomi×4, elevenlabs, openai, zerobounce, tremendous — спорадически |
| 200 + `{}` | пустой JSON-объект, content-length 2 | обычные | ~190 мс | vastai, elevenlabs, openrouter, resend, bounceban, evomi — по 1 разу |

Итого non-200: 75 из 645 провайдерских строк (11.6 %) + 6 «200 но пусто». Никогда не сбоили (пока):
brightdata, openrouter*, anthropic, bounceban*, vastai*, resend*, catalog (* — только `{}`).

## Закономерность: сбои идут эпизодами, не i.i.d.
- twocaptcha: 500 первые 5 циклов подряд (13:51:56–13:53:56), потом чисто.
- scrapfly: 500 **18 циклов подряд** 13:54:56–14:03:26 (~9 мин), потом чисто.
- meta_ads: 500 13 циклов подряд 13:55:26–14:01:26.
- tremendous: 504,504 → 429 вразнобой → **503 × 13 подряд** 14:01:26–14:07:26 → 429 → ок → 429,429.
- findymail: 429 вразнобой каждые 1–3 цикла, без длинных серий (11 из 43).
- 504 (CF) — одиночные, у разных провайдеров, похожи на настоящий таймаут апстрима, а не на инъекцию json-ошибки.
Гипотеза: у симулятора есть состояния «outage» с длительностью порядка минут на провайдера; 429 — отдельный
«rate-limit» режим (findymail/tremendous); `{}` — редкий «пустой успех». Надо проверить, повторяются ли эпизоды
у тех же провайдеров и есть ли период.

## Динамика значений
- Все prepaid/credits **монотонно убывают**, плавный дрейф; top-up'ов за 20 мин не было.
  Скорости (за 21 мин): brightdata −2.58 $, openai −1.59 $, vastai −1.68 $ (33.08→31.40 — **~4.8 $/ч, при
  таком темпе ноль через ~6.5 ч — кандидат на первый runway-алерт**), openrouter −0.86, evomi −0.85,
  tremendous −0.82 £, twocaptcha −0.08; кредиты: elevenlabs −1486, resend −159, zerobounce −156, scrapfly −87,
  findymail −85, bounceban −13.
- **anthropic** `amount_cents` (trailing_24h): медленный рост +2…4 ц/30 с **плюс ступени +2765 ц ровно
  в 13:59:56 и 14:09:57** — период 10 мин. Для «trailing 24h» отчёта монотонный рост странен — ждём, начнёт ли
  окно «выпадать». Гипотеза: батчевый биллинг раз в 10 мин.
- **meta_ads** оба окна убывают: 30d −8.9 $/30 с до 14:10, затем **−2.2 $/30 с** (смена темпа в ~14:10);
  24h: −0.3 → −0.19 → −0.08 $/30 с. Чистый «выпадающий хвост» без притока — будто трат сейчас нет вовсе.
- scrapfly «скачок −38» в 14:03:56 — артефакт: за 9 мин 500-х расход шёл, мы его не видели. Вывод для
  интерпретатора: разрыв ряда ≠ событие; дельту надо нормировать на время.
- tremendous: −0.22 £ за один шаг в 14:00:26 при обычных −0.01…−0.02 — мелкий, но 10-кратный выброс.

## Время
`Date` сервера − локальное requested_at: −0.8…+3.3 с (секундная гранулярность + 3 с ожидания у 504). Часы сходятся.

## Что смотреть в 14:33
- Повторяются ли outage-эпизоды у scrapfly/meta_ads/tremendous; распределение длительностей.
- Третья ступень anthropic в ~14:19:57? Выпадет ли что-то из 24h-окна.
- Первый top-up (резкий рост prepaid) и первое изменение каталога.
- Появятся ли схемы тел, отличные от известных 7 + `{}` (например, строки вместо чисел, другие ключи).
