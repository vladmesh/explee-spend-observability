# Polling notes

Наблюдения за поведением `jobs.explee.com/.../api` во время сбора сырых данных.
Источник данных: `data/raw.sqlite3` (таблица `raw_responses`) и `data/raw_responses.jsonl`,
поллер `explee_test.observability.raw_poller`, интервал 30 с, лог `data/poller.log`.

Формат: один файл на заметку, `YYYY-MM-DD-HHMM-<slug>.md`, UTC. Гипотезы помечены как гипотезы.
